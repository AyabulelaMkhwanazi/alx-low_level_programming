#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 * @argc: argument count
 * @argv: argument vector
 *
 * Desciption - program adds positive numbers
 * Return: 0 (success)
 */

int main(int argc, char *argv[])
{
	int i,j;
	int sum = 0;

	for (i = 0; i < argc; i++)
	{
		if (a
}
